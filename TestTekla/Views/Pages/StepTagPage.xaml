<UserControl x:Class="TeklaApp.Views.StepTagPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignWidth="680" d:DesignHeight="550">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Views/Styles/ModernTheme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" MinWidth="240"/>
            <ColumnDefinition Width="2*" MinWidth="300"/>
        </Grid.ColumnDefinitions>

        <!-- Left: Settings Panel -->
        <Border Grid.Column="0" Style="{StaticResource CardStyle}" Margin="0,0,10,0" VerticalAlignment="Stretch">
            <ScrollViewer Padding="0 0 10 0" VerticalScrollBarVisibility="Auto">
                <StackPanel >
                    <TextBlock Text="TEXT PARAMETERS" FontWeight="Bold" Foreground="#999" FontSize="10" Margin="0,10,0,8"/>

                    <TextBlock Text="Font Height:" Style="{StaticResource LabelTextStyle}"/>
                    <TextBox Name="txtTextHeight" Text="2" Margin="0,0,0,8" TextChanged="OnSettingChanged"/>

                    <TextBlock Text="Font Name:" Style="{StaticResource LabelTextStyle}"/>
                    <TextBox Name="txtFontName" Text="Arial" Margin="0,0,0,8"/>

                    <TextBlock Text="Font Color:" Style="{StaticResource LabelTextStyle}"/>
                    <ComboBox x:Name="cmbTextColor" Margin="0,0,0,8" SelectionChanged="OnSettingChanged">
                        <ComboBoxItem Content="Black"/>
                        <ComboBoxItem Content="White"/>
                        <ComboBoxItem Content="Red"/>
                        <ComboBoxItem Content="Green" IsSelected="True"/>
                        <ComboBoxItem Content="Blue"/>
                        <ComboBoxItem Content="Cyan"/>
                        <ComboBoxItem Content="Magenta"/>
                        <ComboBoxItem Content="Yellow"/>
                    </ComboBox>

                    <TextBlock Text="GEOMETRY PARAMETERS" FontWeight="Bold" Foreground="#999" FontSize="10" Margin="0,10,0,8"/>

                    <TextBlock Text="Surface Length (mm):" Style="{StaticResource LabelTextStyle}"/>
                    <TextBox Name="txtSurfLen" Text="150" Margin="0,0,0,8" TextChanged="OnSettingChanged"/>

                    <TextBlock Text="Step Height (mm):" Style="{StaticResource LabelTextStyle}"/>
                    <TextBox Name="txtStepHeight" Text="70" Margin="0,0,0,8" TextChanged="OnSettingChanged"/>

                    <TextBlock Text="HATCH PARAMETERS" FontWeight="Bold" Foreground="#999" FontSize="10" Margin="0,10,0,8"/>

                    <TextBlock Text="Spacing / Depth (mm):" Style="{StaticResource LabelTextStyle}"/>
                    <TextBox Name="txtHatchLen" Text="70" Margin="0,0,0,8" TextChanged="OnSettingChanged"/>

                    <StackPanel>
                        <TextBlock Text="Pattern Name:" Style="{StaticResource LabelTextStyle}"/>
                        <TextBox Name="txtFillName" Text="ANSI32_A" Margin="0,0,0,8" TextChanged="OnSettingChanged"/>

                        <TextBlock Text="Scale X:" Style="{StaticResource LabelTextStyle}"/>
                        <TextBox Name="txtScaleX" Text="0.05" Margin="0,0,0,12" TextChanged="OnSettingChanged"/>

                        <TextBlock Text="Scale Y:" Style="{StaticResource LabelTextStyle}"/>
                        <TextBox Name="txtScaleY" Text="0.05" Margin="0,0,0,10" TextChanged="OnSettingChanged"/>
                    </StackPanel>

                    <Border Background="#F8F8F8" CornerRadius="4" Margin="0,15,0,0" Padding="10">
                        <TextBlock Name="txtStatus" Text="Ready" TextWrapping="Wrap" Foreground="#444" FontSize="11" FontStyle="Italic"/>
                    </Border>
                </StackPanel>
            </ScrollViewer>

        </Border>

        <!-- Right: Visual Preview Area -->
        <Border Grid.Column="1" Style="{StaticResource CardStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Border Background="#F0F0F0" CornerRadius="4">
                    <Viewbox  Margin="20">
                        <Canvas Name="previewCanvas" Width="400" Height="400" ClipToBounds="True"/>
                    </Viewbox>
                </Border>

                <Button Margin="10" Grid.Row="1" Width="200" Content="RUN GENERATION" Height="50" FontSize="13" Click="BtnRun_Click" Style="{DynamicResource PrimaryButtonStyle}"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
