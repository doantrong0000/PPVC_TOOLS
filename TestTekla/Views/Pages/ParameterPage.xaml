<UserControl x:Class="TeklaApp.Views.ParameterPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Views/Styles/ModernTheme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <TextBlock Text="Parameter Explorer" Style="{StaticResource HeaderTextStyle}"/>

        <Button Grid.Row="1" Content="SELECT &amp; READ PARAMETERS" Height="45" Margin="0,0,0,15" 
                Click="BtnRead_Click" Style="{StaticResource PrimaryButtonStyle}"/>
        
        <Border Grid.Row="2" Style="{StaticResource CardStyle}">
            <TextBox Name="txtInfo" BorderThickness="0" IsReadOnly="True" 
                     VerticalScrollBarVisibility="Auto" AcceptsReturn="True" 
                     FontFamily="Consolas" FontSize="12"/>
        </Border>
    </Grid>
</UserControl>
